<Page x:Class="KMA.APZRPMJ2018.WalletSimulator.Views.Wallet.AlarmView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:KMA.APZRPMJ2018.WalletSimulator.Views.Wallet"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AlarmView">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Button Grid.Row="0" Grid.Column="0" Content="Button" Margin="10,20"/>
        <Button Grid.Row="1" Grid.Column="0" Content="Button" Margin="10,20"/>
        <ScrollViewer Grid.Column="1" Grid.Row="0" Grid.RowSpan="3" Margin="30,30">
            <WrapPanel>
                <WrapPanel>
                    <ComboBox ItemsSource="{Binding Hour}">

                    </ComboBox>

                    <ComboBox ItemsSource="{Binding Minute}">

                    </ComboBox>
                </WrapPanel>
            </WrapPanel>
        </ScrollViewer>
    </Grid>
</Page>
